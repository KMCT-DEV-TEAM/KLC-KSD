export const metadata = {
  title: 'Admissions',
  description: 'Admissions for KMCT College of Legal Studies',
};

export default function AdmissionsLayout({ children }) {
  return <>{children}</>;
}
